extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="addtask",method="POST")
   p Title
   input(name="title")
   p Authore
   input(name="author")
   p Description
   textarea(name="description")
   p Type
   input(name="type")
   p Data
   textarea(name="data")
   br
   button(type="submit") Submit
